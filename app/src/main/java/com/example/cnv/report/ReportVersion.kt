package com.example.cnv.report

/**
 * Maintenance Report format version (STEP 19).
 * Increment when report layout / fields change — not when session data changes.
 */
object ReportVersion {
    const val CURRENT = 1
}
