package com.example.cnv.heatmap

/**
 * HeatMap visualization modes. STEP 12-2: SHOCK is active;
 * COVERAGE / CONFIDENCE are reserved for STEP 12-3+.
 */
enum class HeatMapMode {
    SHOCK,
    COVERAGE,
    CONFIDENCE,
}
