package com.example.cnv.rule

/**
 * Future Rule Import / Export / custom Factory·Drawing·Zone rules (STEP 18 extension point).
 * Consumers must use [RuleDefinitionRepository] — not hardcode rules in UI.
 */
object RuleFutureExtension {
    // Import / Export / user-defined rules will attach here without changing Engine API.
}
